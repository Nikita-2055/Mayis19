#include<stdio.h>
int function(char *str){
    int count = 0;
    for(int i = 0; str[i] != '\0'; ++i){
        count++;
    }
    return count;
}
int main(){
    char str[] = "hello";
    int res = function(str);
    printf("result: %d\n",res);
    return 0;
}
